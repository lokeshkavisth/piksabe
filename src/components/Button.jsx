import React from "react";

const Button = () => {
  return (
    <button
      type="submit"
      className="px-6 py-2 rounded-full bg-emerald-400 shadow-sm hover:bg-emerald-500 transition-all font-semibold"
    >
      Button
    </button>
  );
};

export default Button;
